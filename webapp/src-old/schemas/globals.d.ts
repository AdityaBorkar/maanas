type ProjectType = {
  id: string
  title: string
  // icon: string
  // tabs: never[]
  // index: number
  // archived: boolean
  // createdOn: number
}

type TaskType = {
  id: string
  name: string
  completed: boolean
}
